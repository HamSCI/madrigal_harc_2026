# Figure and analysis environment for HamSCI-MOVIE-CINEMA.
#
#   /opt/local/bin/python3.13 -m venv .venv
#   .venv/bin/pip install -r analysis/requirements.txt
#   .venv/bin/pip install -e "analysis/wspr-station-census[geomag]"   # census CLI
#
# aacgmv2 is NOT optional here. maps.cgm_latitude_grid returns None without it and
# the geomagnetic latitude contours are then silently dropped from the figures while
# the legend still advertises them. It needs a C compiler to build.
aacgmv2==2.7.1
Cartopy==0.25.0
certifi==2026.7.22
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pyparsing==3.3.2
pyproj==3.7.2
pyshp==3.1.6
python-dateutil==2.9.0.post0
shapely==2.1.2
six==1.17.0
